// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/custom_dog_server/custom_dog_server.proto)

#pragma once

#include <array>
#include <cmath>
#include <functional>
#include <limits>
#include <memory>
#include <string>
#include <utility>
#include <vector>


#include "server_plugin_base.h"

#include "handle.h"

namespace mavsdk {


class ServerComponent;
class CustomDogServerImpl;

/**
 * @brief This service can be used to manage the custom dog server protocol.
 * Server side: receives control commands, joy data, and dog model from client.
 *
 * Note: This uses pure gRPC (no MAVLink) for communication.
 */
class CustomDogServer : public ServerPluginBase {
public:

    /**
     * @brief Constructor. Creates the plugin for a ServerComponent instance.
     *
     * The plugin is typically created as shown below:
     *
     *     ```cpp
     *     auto custom_dog_server = CustomDogServer(server_component);
     *     ```
     *
     * @param server_component The ServerComponent instance associated with this server plugin.
     */
    explicit CustomDogServer(std::shared_ptr<ServerComponent> server_component);


    /**
     * @brief Destructor (internal use only).
     */
    ~CustomDogServer() override;


    /**
     * @brief Dog model enum
     */
    enum class DogModel {
        Undefined, /**< @brief. */
        DeeproboticsX30, /**< @brief Deeprobotics X30. */
        ZsiZsl1, /**< @brief ZSI ZSL-1. */
        AstrallA01, /**< @brief Astrall A01. */
    };

    /**
     * @brief Stream operator to print information about a `CustomDogServer::DogModel`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CustomDogServer::DogModel const& dog_model);

    /**
     * @brief Control command enum (bitmask values for capability check).
     * Use bitwise AND to check if a command is available.
     */
    enum class ControlCommand {
        Undefined, /**< @brief. */
        StandUp, /**< @brief 1 << 0. */
        LieDown, /**< @brief 1 << 1. */
        Crouch, /**< @brief 1 << 2. */
        EnterPoseControl, /**< @brief 1 << 3. */
        EnterMoveControl, /**< @brief 1 << 4. */
        EmergencyStop, /**< @brief 1 << 5. */
    };

    /**
     * @brief Stream operator to print information about a `CustomDogServer::ControlCommand`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CustomDogServer::ControlCommand const& control_command);

    /**
     * @brief Possible feedback results for custom dog respond command.
     */
    enum class CustomDogServerFeedback {
        Unknown, /**< @brief Unknown. */
        Ok, /**< @brief Ok. */
        Busy, /**< @brief Busy. */
        Failed, /**< @brief Failed. */
    };

    /**
     * @brief Stream operator to print information about a `CustomDogServer::CustomDogServerFeedback`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CustomDogServer::CustomDogServerFeedback const& custom_dog_server_feedback);





    /**
     * @brief Possible results returned for CustomDog commands.
     */
    enum class Result {
        Unknown, /**< @brief Unknown result. */
        Success, /**< @brief Command executed successfully. */
        InProgress, /**< @brief Command in progress. */
        Busy, /**< @brief System is busy and rejected command. */
        Denied, /**< @brief System denied the command. */
        Error, /**< @brief An error has occurred while executing the command. */
        Timeout, /**< @brief Command timed out. */
        WrongArgument, /**< @brief Command has wrong argument(s). */
        NoSystem, /**< @brief No system connected. */
        ProtocolUnsupported, /**< @brief Definition file protocol not supported. */
    };

    /**
     * @brief Stream operator to print information about a `CustomDogServer::Result`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CustomDogServer::Result const& result);





    /**
     * @brief Joy data wrapper message
     */
    struct JoyData {
        
        float lx{}; /**< @brief Left joystick X axis, range -1.0~1.0 */
        float ly{}; /**< @brief Left joystick Y axis, range -1.0~1.0 */
        float rx{}; /**< @brief Right joystick X axis, range -1.0~1.0 */
        float ry{}; /**< @brief Right joystick Y axis, range -1.0~1.0 */
    };

    /**
     * @brief Equal operator to compare two `CustomDogServer::JoyData` objects.
     *
     * @return `true` if items are equal.
     */
    friend bool operator==(const CustomDogServer::JoyData& lhs, const CustomDogServer::JoyData& rhs);

    /**
     * @brief Stream operator to print information about a `CustomDogServer::JoyData`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CustomDogServer::JoyData const& joy_data);


    /**
     * @brief Callback type for asynchronous CustomDogServer calls.
     */
    using ResultCallback = std::function<void(Result)>;




        

    /**
     * @brief Callback type for subscribe_control_command.
     */
    using ControlCommandCallback = std::function<void(Result, ControlCommand)>;

    /**
     * @brief Handle type for subscribe_control_command.
     */
    using ControlCommandHandle = Handle<Result, ControlCommand>;

    /**
     * @brief Subscribe to receive control commands from client.
     * Server will receive commands like ENTER_STAND_UP, LIE_DOWN, etc.
     * Server streams back the execution result to the client.
     */
    ControlCommandHandle subscribe_control_command(const ControlCommandCallback& callback);

    /**
     * @brief Unsubscribe from subscribe_control_command
     */
    void unsubscribe_control_command(ControlCommandHandle handle);

        






        

    /**
     * @brief Callback type for subscribe_joy_data.
     */
    using JoyDataCallback = std::function<void(JoyData)>;

    /**
     * @brief Handle type for subscribe_joy_data.
     */
    using JoyDataHandle = Handle<JoyData>;

    /**
     * @brief Subscribe to receive joy data from client.
     * Server will receive continuous lx, ly, rx, ry values.
     */
    JoyDataHandle subscribe_joy_data(const JoyDataCallback& callback);

    /**
     * @brief Unsubscribe from subscribe_joy_data
     */
    void unsubscribe_joy_data(JoyDataHandle handle);

        






        

    /**
     * @brief Callback type for subscribe_dog_model.
     */
    using DogModelCallback = std::function<void(Result, DogModel)>;

    /**
     * @brief Handle type for subscribe_dog_model.
     */
    using DogModelHandle = Handle<Result, DogModel>;

    /**
     * @brief Subscribe to receive dog model selection from client.
     * Server will receive the selected dog model (e.g., X30, ZSI ZSL-1).
     * Server will also push the capability (supported commands) of the selected dog model.
     */
    DogModelHandle subscribe_dog_model(const DogModelCallback& callback);

    /**
     * @brief Unsubscribe from subscribe_dog_model
     */
    void unsubscribe_dog_model(DogModelHandle handle);

        








    /**
     * @brief Respond to select dog model request from SubscribeDogModel.
     *
     * This function is blocking.
     *
     * @return Result of request.
     */
    Result respond_dog_model(CustomDogServerFeedback dog_model_feedback) const;




    /**
     * @brief Directly publish current available commands to client.
     * Server calls this to send available_cmd_bitmask to client.
     *
     * This function is non-blocking. See 'publish_current_available_command' for the blocking counterpart.
     */
    void publish_current_available_command_async(uint32_t available_cmd_bitmask, const ResultCallback callback);



    /**
     * @brief Directly publish current available commands to client.
     * Server calls this to send available_cmd_bitmask to client.
     *
     * This function is blocking. See 'publish_current_available_command_async' for the non-blocking counterpart.
     *
     * @return Result of request.
     */
    Result publish_current_available_command(uint32_t available_cmd_bitmask) const;




    /**
     * @brief Directly publish dog ability to client.
     * Server calls this to send ability_bitmask to client.
     *
     * This function is non-blocking. See 'publish_dog_ability' for the blocking counterpart.
     */
    void publish_dog_ability_async(uint32_t ability_bitmask, const ResultCallback callback);



    /**
     * @brief Directly publish dog ability to client.
     * Server calls this to send ability_bitmask to client.
     *
     * This function is blocking. See 'publish_dog_ability_async' for the non-blocking counterpart.
     *
     * @return Result of request.
     */
    Result publish_dog_ability(uint32_t ability_bitmask) const;




    /**
     * @brief Publish battery level to client.
     * Server calls this to send current battery_level to client.
     *
     * This function is non-blocking. See 'publish_battery_level' for the blocking counterpart.
     */
    void publish_battery_level_async(uint32_t battery_level, const ResultCallback callback);



    /**
     * @brief Publish battery level to client.
     * Server calls this to send current battery_level to client.
     *
     * This function is blocking. See 'publish_battery_level_async' for the non-blocking counterpart.
     *
     * @return Result of request.
     */
    Result publish_battery_level(uint32_t battery_level) const;




        

    /**
     * @brief Callback type for subscribe_dog_ability.
     */
    using DogAbilityCallback = std::function<void(uint32_t)>;

    /**
     * @brief Handle type for subscribe_dog_ability.
     */
    using DogAbilityHandle = Handle<uint32_t>;

    /**
     * @brief Subscribe to provide dog ability when client asks.
     * Server will call this callback when it receives MAV_CMD_CUSTOM_DOG_ASK_ABILITY.
     * The callback should set the ability_bitmask (supported commands).
     */
    DogAbilityHandle subscribe_dog_ability(const DogAbilityCallback& callback);

    /**
     * @brief Unsubscribe from subscribe_dog_ability
     */
    void unsubscribe_dog_ability(DogAbilityHandle handle);

        






        

    /**
     * @brief Callback type for subscribe_current_available_command.
     */
    using CurrentAvailableCommandCallback = std::function<void(uint32_t)>;

    /**
     * @brief Handle type for subscribe_current_available_command.
     */
    using CurrentAvailableCommandHandle = Handle<uint32_t>;

    /**
     * @brief Subscribe to provide current available commands when client asks.
     * Server will call this callback when it receives MAV_CMD_CUSTOM_DOG_CURRENT_AVAILABLE_CMD.
     * The callback should set the available_cmd_bitmask.
     */
    CurrentAvailableCommandHandle subscribe_current_available_command(const CurrentAvailableCommandCallback& callback);

    /**
     * @brief Unsubscribe from subscribe_current_available_command
     */
    void unsubscribe_current_available_command(CurrentAvailableCommandHandle handle);

        






    /**
     * @brief Copy constructor.
     */
    CustomDogServer(const CustomDogServer& other);

    /**
     * @brief Equality operator (object is not copyable).
     */
    const CustomDogServer& operator=(const CustomDogServer&) = delete;

private:
    /** @private Underlying implementation, set at instantiation */
    std::unique_ptr<CustomDogServerImpl> _impl;
};

} // namespace mavsdk