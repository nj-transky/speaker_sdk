// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/custom_dog/custom_dog.proto)

#pragma once

#include <array>
#include <cmath>
#include <functional>
#include <limits>
#include <memory>
#include <string>
#include <utility>
#include <vector>


#include "plugin_base.h"

#include "handle.h"

namespace mavsdk {


class System;class CustomDogImpl;

/**
 * @brief This service can be used to manage the custom dog control protocol.
 */
class CustomDog : public PluginBase {
public:

    /**
     * @brief Constructor. Creates the plugin for a specific System.
     *
     * The plugin is typically created as shown below:
     *
     *     ```cpp
     *     auto custom_dog = CustomDog(system);
     *     ```
     *
     * @param system The specific system associated with this plugin.
     */
    explicit CustomDog(System& system); // deprecated

    /**
     * @brief Constructor. Creates the plugin for a specific System.
     *
     * The plugin is typically created as shown below:
     *
     *     ```cpp
     *     auto custom_dog = CustomDog(system);
     *     ```
     *
     * @param system The specific system associated with this plugin.
     */
    explicit CustomDog(std::shared_ptr<System> system); // new


    /**
     * @brief Destructor (internal use only).
     */
    ~CustomDog() override;


    /**
     * @brief Dog model enum
     */
    enum class DogModel {
        Undefined, /**< @brief. */
        DeeproboticsX30, /**< @brief Deeprobotics X30. */
        ZsiZsl1, /**< @brief ZSI ZSL-1. */
        AstrallA01, /**< @brief Astrall A01. */
    };

    /**
     * @brief Stream operator to print information about a `CustomDog::DogModel`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CustomDog::DogModel const& dog_model);

    /**
     * @brief Control command enum (bitmask values for capability check).
     * Use bitwise AND to check if a command is available.
     * Example: if (available_cmd & CONTROL_COMMAND_ENTER_STAND_UP) { }
     */
    enum class ControlCommand {
        Undefined, /**< @brief. */
        StandUp, /**< @brief 1 << 0. */
        LieDown, /**< @brief 1 << 1. */
        Crouch, /**< @brief 1 << 2. */
        EnterPoseControl, /**< @brief 1 << 3. */
        EnterMoveControl, /**< @brief 1 << 4. */
        EmergencyStop, /**< @brief 1 << 5. */
    };

    /**
     * @brief Stream operator to print information about a `CustomDog::ControlCommand`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CustomDog::ControlCommand const& control_command);





    /**
     * @brief Possible results returned for CustomDog commands.
     */
    enum class Result {
        Unknown, /**< @brief Unknown result. */
        Success, /**< @brief Command executed successfully. */
        InProgress, /**< @brief Command in progress. */
        Busy, /**< @brief System is busy and rejected command. */
        Denied, /**< @brief System denied the command. */
        Error, /**< @brief An error has occurred while executing the command. */
        Timeout, /**< @brief Command timed out. */
        WrongArgument, /**< @brief Command has wrong argument(s). */
        NoSystem, /**< @brief No system connected. */
        ProtocolUnsupported, /**< @brief Definition file protocol not supported. */
    };

    /**
     * @brief Stream operator to print information about a `CustomDog::Result`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CustomDog::Result const& result);



    /**
     * @brief Callback type for asynchronous CustomDog calls.
     */
    using ResultCallback = std::function<void(Result)>;




    /**
     * @brief Send control command to dog (e.g., stand up, lie down, crouch, etc).
     * Server will execute the command and return the result.
     *
     * This function is non-blocking. See 'send_control_command' for the blocking counterpart.
     */
    void send_control_command_async(ControlCommand command, const ResultCallback callback);



    /**
     * @brief Send control command to dog (e.g., stand up, lie down, crouch, etc).
     * Server will execute the command and return the result.
     *
     * This function is blocking. See 'send_control_command_async' for the non-blocking counterpart.
     *
     * @return Result of request.
     */
    Result send_control_command(ControlCommand command) const;




    /**
     * @brief Send joy data to dog (lx, ly, rx, ry).
     * Uses client streaming - client sends multiple joy data messages.
     * No response needed for each message (fire and forget).
     *
     * This function is non-blocking. See 'send_joy_data' for the blocking counterpart.
     */
    void send_joy_data_async(float lx, float ly, float rx, float ry, const ResultCallback callback);



    /**
     * @brief Send joy data to dog (lx, ly, rx, ry).
     * Uses client streaming - client sends multiple joy data messages.
     * No response needed for each message (fire and forget).
     *
     * This function is blocking. See 'send_joy_data_async' for the non-blocking counterpart.
     *
     * @return Result of request.
     */
    Result send_joy_data(float lx, float ly, float rx, float ry) const;




    /**
     * @brief Select dog model (e.g., Deeprobotics X30, ZSI ZSL-1).
     * This is used by the server to know which dog model is connected.
     * Server will return the capability (supported commands) of the selected dog model.
     *
     * This function is non-blocking. See 'select_dog_model' for the blocking counterpart.
     */
    void select_dog_model_async(DogModel dog_model, const ResultCallback callback);



    /**
     * @brief Select dog model (e.g., Deeprobotics X30, ZSI ZSL-1).
     * This is used by the server to know which dog model is connected.
     * Server will return the capability (supported commands) of the selected dog model.
     *
     * This function is blocking. See 'select_dog_model_async' for the non-blocking counterpart.
     *
     * @return Result of request.
     */
    Result select_dog_model(DogModel dog_model) const;




        

    /**
     * @brief Callback type for subscribe_current_available_command.
     */
    using CurrentAvailableCommandCallback = std::function<void(uint32_t)>;

    /**
     * @brief Handle type for subscribe_current_available_command.
     */
    using CurrentAvailableCommandHandle = Handle<uint32_t>;

    /**
     * @brief Subscribe to receive real-time available commands from server.
     * Server will push available_cmd_bitmask when the dog state changes.
     * Use bitwise AND to check if a command is available.
     */
    CurrentAvailableCommandHandle subscribe_current_available_command(const CurrentAvailableCommandCallback& callback);

    /**
     * @brief Unsubscribe from subscribe_current_available_command
     */
    void unsubscribe_current_available_command(CurrentAvailableCommandHandle handle);

        



    /**
     * @brief Poll for 'uint32_t' (blocking).
     *
     * @return One uint32_t update.
     */
    uint32_t current_available_command() const;




    /**
    * @brief Callback type for get_dog_ability_async.
    */
    using GetDogAbilityCallback = std::function<void(Result, uint32_t)>;

    /**
     * @brief Get the dog's ability (supported commands bitmask).
     * Client sends this to ask what commands the dog supports.
     * Server responds via SUPPORT_ABILITY MAVLink message (51851).
     *
     * This function is non-blocking. See 'get_dog_ability' for the blocking counterpart.
     */
    void get_dog_ability_async(const GetDogAbilityCallback callback);



    /**
     * @brief Get the dog's ability (supported commands bitmask).
     * Client sends this to ask what commands the dog supports.
     * Server responds via SUPPORT_ABILITY MAVLink message (51851).
     *
     * This function is blocking. See 'get_dog_ability_async' for the non-blocking counterpart.
     *
     * @return Result of request.
     */
    std::pair<Result, uint32_t> get_dog_ability() const;




    /**
    * @brief Callback type for get_current_available_command_async.
    */
    using GetCurrentAvailableCommandCallback = std::function<void(Result, uint32_t)>;

    /**
     * @brief Get current available commands bitmask.
     * Client sends this to query current available commands.
     * Server responds via CURRENT_AVAILABLE_CMD MAVLink message (51852).
     *
     * This function is non-blocking. See 'get_current_available_command' for the blocking counterpart.
     */
    void get_current_available_command_async(const GetCurrentAvailableCommandCallback callback);



    /**
     * @brief Get current available commands bitmask.
     * Client sends this to query current available commands.
     * Server responds via CURRENT_AVAILABLE_CMD MAVLink message (51852).
     *
     * This function is blocking. See 'get_current_available_command_async' for the non-blocking counterpart.
     *
     * @return Result of request.
     */
    std::pair<Result, uint32_t> get_current_available_command() const;




        

    /**
     * @brief Callback type for subscribe_battery_level.
     */
    using BatteryLevelCallback = std::function<void(uint32_t)>;

    /**
     * @brief Handle type for subscribe_battery_level.
     */
    using BatteryLevelHandle = Handle<uint32_t>;

    /**
     * @brief Subscribe to receive battery level updates from server.
     * Server pushes battery_level whenever it changes.
     */
    BatteryLevelHandle subscribe_battery_level(const BatteryLevelCallback& callback);

    /**
     * @brief Unsubscribe from subscribe_battery_level
     */
    void unsubscribe_battery_level(BatteryLevelHandle handle);

        



    /**
     * @brief Poll for 'uint32_t' (blocking).
     *
     * @return One uint32_t update.
     */
    uint32_t battery_level() const;




    /**
     * @brief Copy constructor.
     */
    CustomDog(const CustomDog& other);

    /**
     * @brief Equality operator (object is not copyable).
     */
    const CustomDog& operator=(const CustomDog&) = delete;

private:
    /** @private Underlying implementation, set at instantiation */
    std::unique_ptr<CustomDogImpl> _impl;
};

} // namespace mavsdk